__pycache__/
*.pyc

# Resultados de execução — não são código-fonte, e a pasta é muito grande (dezenas de GB)
JOBS/

# Backups manuais antigos — substituídos pelo histórico do git
ANTIGOS/

# Saída do build do pacote .deb (packaging/deb/build.sh) — binário gerado, não fonte
packaging/dist/
